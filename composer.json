{
    "name": "hexlet/code",
    "require": {
        "slim/slim": "^4.0"
    },
    "require-dev": {
        "squizlabs/php_codesniffer": "^4.0"
    },
    "scripts": {
        "lint": "phpcs --standard=PSR12 --extensions=php public/",
        "lint-fix": "phpcbf --standard=PSR12 --extensions=php public/"
    }
}
