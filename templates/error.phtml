<div class="text-center py-5">
    <h1 class="display-1 text-danger"><?= $statusCode ?? 404 ?></h1>
    <h2 class="mb-4"><?= htmlspecialchars($message ?? 'Страница не найдена') ?></h2>
    <p class="lead">
        Вернитесь на <a href="<?= $router->urlFor('home') ?>">главную страницу</a>
    </p>
</div>
